import { Event } from "@/types";

/**
 * Intentionally empty — see the note in `blogs.ts`.
 *
 * The four events here were invented, with picsum.photos posters and dates
 * that had already passed. Events are added in the admin panel.
 */
export const events: Event[] = [];
