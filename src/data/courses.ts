import { Course } from "@/types";

/**
 * Intentionally empty — see the note in `blogs.ts`.
 *
 * The six courses that used to live here were invented, down to the
 * instructors, and every thumbnail was a picsum.photos placeholder. Courses
 * are added in the admin panel.
 */
export const courses: Course[] = [];
